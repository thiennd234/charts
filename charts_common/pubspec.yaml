name: charts_common
version: 0.12.0
description: A common library for charting packages.
homepage: https://github.com/google/charts

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  collection: ^1.14.5
  intl: ">=0.17.0 < 0.18.0"
  logging: ^1.0.2
  meta: ^1.1.1
  vector_math: ^2.1.1

dev_dependencies:
  mockito: ^5.1.0
  test: ^1.19.5
